# CLAUDE.md — 에이전트 컨텍스트 허브

이 파일은 Claude(및 모든 AI 에이전트)가 이 레포에서 **가장 먼저 읽는 맥락 허브**입니다.
원칙: **적혀 있지 않으면 에이전트에게는 존재하지 않습니다.** 결정·규칙·맥락은 여기에 글로 남깁니다.

---

## 🌟 북극성 (이 프로젝트가 향하는 목표)

**FC ONLINE 감독모드 자동매크로(mAuto)를 "구매자는 사용만, 내부 접근·복제·디컴파일은 불가"한 상태로 안정 판매한다.**

성공 지표:
1. 클린 PC에서 설치(setup.exe) → 더블클릭 한 번으로 실행 (무반응/크래시 0)
2. 게임 창이 **비활성(백그라운드)일 때도 100%** 입력·인식 동작
3. 빌드 산출물에서 로직·이미지가 노출되지 않음(Nuitka 컴파일 + 자산 임베드)

스코프 밖(하지 않을 것):
- 탐지 회피(안티치트 우회)·대량 배포·악용 목적 기능
- 마우스 이동 **궤적 변경** (아래 불변식 참고)
- 비활성 100% 동작을 깨는 변경

---

## 📦 프로젝트 개요

- **무엇:** FC ONLINE 감독모드 화면을 인식해 자동 클릭/키 입력하고, 등수·점수·티어를 OCR해 디스코드로 보고하는 Windows 데스크톱 매크로 + 라이센스/디스코드 봇 백엔드.
- **기술 스택:**
  - 매크로 본체: **Python 3** — tkinter(GUI), OpenCV(`opencv-python-headless`), numpy, **winocr**(Windows 내장 OCR), **vgamepad**(ViGEm 가상 패드), **pywin32**, **windows-capture(WGC)**, pyautogui, pillow
  - 빌드: **Nuitka**(디컴파일 방어, `--onefile` 폴더형) + **PyInstaller** 폴백, **Inno Setup**(setup.exe)
  - 디스코드 봇: **Node.js / discord.js v14** (Railway 호스팅)
  - 라이센스 서버: **Vercel 서버리스 + Firebase Firestore**
- **레포 구조:** ⚠️ **git 루트는 상위 폴더**(`/Users/m/Documents/GitHub/macro`), 프로젝트 실체는 `macro/macro`. 추적 경로는 전부 `macro/...` 접두사 (예: `macro/macroapp/gui.py`).

```
macro/                         ← git 루트 (상위)
└─ macro/                      ← 작업 디렉터리(여기서 작업)
   ├─ macroapp/                — 매크로 Python 패키지
   │  ├─ app.py / gui.py       — 진입·tkinter GUI + 자동화 루프
   │  ├─ config.py             — 상수/타깃 로딩/자산
   │  ├─ capture.py window.py  — WGC 캡처·창 관리(클라이언트 영역)
   │  ├─ matching.py           — 2단계 템플릿 매칭(다운스케일→ROI)
   │  ├─ ocr.py                — 등수/티어/점수 OCR(winocr) + 컨센서스
   │  ├─ input_gamepad.py      — vgamepad 입력(RLock 직렬화)
   │  ├─ input_mouse.py        — 마우스 곡선(베지어) 클릭 ⚠️궤적 불변
   │  ├─ input_message.py      — PostMessage 키/마우스
   │  ├─ license_client.py     — HWID·라이센스 검증·상태 전송
   │  └─ winapi.py paths.py …  — 가드된 Windows 의존성/경로
   ├─ discord-bot/bot.js       — 디스코드 봇(discord.js v14)
   ├─ license-server/api/*.js  — Vercel 서버리스(verify/status/admin/…)
   ├─ build.bat setup.iss      — 빌드·설치 패키징
   ├─ gen_assets.py launcher.py macro_main.py
   └─ version.txt targets.json target_*.png
```

---

## ▶️ 자주 쓰는 명령어

⚠️ **빌드와 매크로 실행은 Windows 전용.** Mac에선 winocr·vgamepad·WGC·pywin32가 없어 검증만 가능(아래 의존성은 모두 가드되어 import는 됨).

```bash
# 빌드 (Windows에서만) — Nuitka 3바이너리 + ISCC로 setup.exe 생성
build.bat            # version.txt 읽어 setup.iss 동기화 → gen_assets → 컴파일 → 빌드

# Mac에서 가능한 검증(정적·순수 로직) — 정식 테스트 스위트는 없음
python3 -m compileall macroapp/*.py        # 문법(=build.bat의 '문법검사' 대응)
python3 -m pyflakes macroapp/*.py          # 미정의/미사용 점검
python3 -c "import macroapp.gui, macroapp.ocr, macroapp.config"   # import 스모크
# 순수 로직(파서/컨센서스/전처리/타이밍)은 winocr 없이 단위 검증 가능 — ocr.py는 일부러 분리해 둠
```

---

## ✅ 작업 완료 기준 (Definition of Done)

- [ ] `compileall` + `pyflakes` 통과 (내 변경이 만든 경고 0)
- [ ] **Windows 전용 기능은 순수 로직을 분리**해 Mac에서 단위 검증(정규식·투표·전처리·타이밍 시뮬레이션)
- [ ] Windows 의존성(winocr/vgamepad/cv2/pywin32)이 없거나 실패해도 **매크로 본체에 영향 0**(전부 가드)
- [ ] 불변식 보존: 마우스 궤적 바이트 동일, 비활성 100%, 이미지 매칭 경로
- [ ] 위험·복합 변경은 **워크플로 적대적 리뷰(Doer–Verifier)** 후 confirmed만 반영
- [ ] 커밋 메시지에 "무엇을·왜" 명시
- [ ] winocr/입력/캡처 등 Mac에서 못 돌리는 부분은 **"사용자가 Windows에서 빌드·테스트" 필요**라고 명확히 전달

---

## 🧭 코딩 규칙 / 컨벤션

- **언어:** 코드 주석·로그·커밋 메시지·UI는 **한국어**. Python 4-space 들여쓰기.
- **커밋:** Conventional Commits 접두사(한국어 본문) — `feat:`, `fix:`, `perf:`, `tune:`, `chore:`, `docs:`. 푸터에 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **Windows 의존성 접근:** 직접 import 금지 → `from macroapp import winapi` 후 `winapi.win32gui`, `winapi.vg` 등 **속성으로 접근**(키스톤 가드). 없으면 `None`이라 Mac import가 안 깨짐.
- **하지 말 것:**
  - 디스코드 토큰·`BOT_API_KEY` 등 **비밀키 하드코딩/입력칸 타이핑 금지** — 사용자가 직접 입력.
  - `input_mouse.py`의 베지어 궤적·RNG 추출 순서 변경 금지.
  - 사용자 동의 없는 배포(Railway/Vercel)·버전 올림·릴리스.

---

## 🚦 자율성 & 사람 개입 (Trust Ladder)

에이전트가 알아서 해도 되는 것:
- 버그 수정, 성능/정확도 개선, 리팩터링, 주석/문서, 순수 로직 추가와 그 Mac 검증, 커밋(브랜치/요청 시 푸시)

반드시 사람이 하는 것:
- **빌드·릴리스**(Windows에서 사용자가 `build.bat`), **배포**(Railway 봇 / Vercel 서버), 버전 올림
- 의존성 추가/삭제, Firestore 스키마·라이센스 정책 변경
- 비밀키 입력, GitHub **push**(사용자는 GitHub Desktop 사용 + 가끔 커밋 사이에 `git pull --ff` 함 → 푸시 전 `git rev-list --count origin/main..HEAD` 확인)

판단이 애매하면: 추측으로 빌드 사이클 낭비시키지 말고 **질문을 모아** 올린다(특히 화면 좌표/레이아웃처럼 사용자만 아는 값).

---

## 📝 결정 로그 (왜 이렇게 했는지 — 최신순)

- `2026-08-22` **자동 종료 재시도 — '나가기로 확정된 판'은 나갈 때까지 다시 시도한다.**
  사용자 실측: 종료가 발동해 일시정지→재시작까지 갔는데 **마무리(확인 버튼)가 실패해 판이
  안 나가지는** 경우가 있다. 그 판은 이미 나가기로 확정된 판이므로 확정적으로 나가야 한다.
  ① **기준 시각은 '되살아난 시점'(STATE_FOLLOWUP 진입)이다.** 처음엔 '시도 완료'부터 쟀는데,
  마무리는 확인 버튼을 못 찾으면 `EXIT_TIMEOUT_SECONDS=40`초까지 돈다 — **재시도가 겨냥하는
  바로 그 실패**라서, 완료 기준이면 재시도가 재시작+70초로 밀린다(리뷰 미검증 목록에서
  발견). 겹침 걱정은 불필요했다: 재시작~완료 내내 `_exit_gate` 가 서 있어
  `_observe_match_score` 가 조기 리턴하므로 tick 자체가 안 돈다.
  ② **'같은 판' 보장은 `LossTracker.latched`**(스코어보드 60초 연속 부재로만 풀림). 판이
  끝났으면(=나갔으면) 래치가 풀려 재시도가 자동 해제된다. 마감 시점 스코어가 열세면
  재시도, 회복/오독이면 해제, 못 읽으면(리플레이 가림) 3초 뒤 재확인.
  ③ **쿼터를 새로 세지 않는다.** 이미 확정된 판의 재시도라 비율 계산과 무관하다
  (`register_loss` 를 안 부르고 `ui_queue` 로 직행). 판당 최대 `AUTO_EXIT_RETRY_MAX=5`.
  ④ **한도를 tick 과 arm 양쪽에 둔다** — arm 에만 두면 넘긴 요청이 드롭됐을 때 완료-무장이
  안 돌아 한도 검사를 통째로 건너뛴다. **`_auto_exit_current` 는 실제로 시작된 경우에만**
  세운다(`run_quick_exit` 이 bool 반환) — 만료·busy·start 실패로 조용히 돌아설 때 True 로
  남으면 나중의 다른 종료 완료가 이 판을 '자동 종료였다'로 오귀속한다.
  ⑤ **적대적 리뷰(3관점·15에이전트) 확정 0건.** 반증된 상위 지적의 공통 뿌리는
  '비게임 화면(결과·로비)의 스코어 영역이 열세 튜플로 읽힌다'는 **미재현 전제**였다 —
  레포의 실물 프레임 34장에서 열세 튜플은 전부 인게임 캡처였고, 설령 오독해도 실제 클릭은
  `ConfirmSequence` 가 `target_H` 를 **본 프레임에서만** press 를 내므로 한 번 더 막힌다.
  ⚠️ 잔여(문서화만): 재시도 경로는 정상 경로의 두 방어(`confirm_count` 연속 확인·선행
  비열세 스코어)를 안 탄다 — 단일 프레임 `classify` 다. 이미 나가기로 확정한 판을 한 번 더
  나가려는 것이라 오발 피해가 대칭이 아니라서 그대로 뒀다. 실기에서 '엉뚱한 재시도' 로그가
  보이면 그때 연속 확인을 넣는다.

- `2026-08-22` **1.0.39 릴리스 + 전술창 '−' 자동 접기(target_J) + 홈 화면 티어·점수·순위 OCR.**
  ① 릴리스 절차는 `gh release create v1.0.39 dist/*.exe` → `sign_release.py --post`(ADMIN_KEY 환경변수)
  ×2 제품 → `/api/version?product=` 확인. **빌드는 사용자 PC 의 `macro_pro.exe`(관리자 권한) 가
  dist 를 잡고 있어 세 번 막혔다** — taskkill/Stop-Process 는 access denied, UAC 승격
  (`Start-Process -Verb RunAs`)으로만 끌 수 있었다. 한 번은 Nuitka·PyInstaller 가 시작 직후 둘 다
  메시지 없이 죽었다(여유 메모리 2GB/16GB — 게임+mAuto 2개+리뷰 에이전트 14개) → 재시도로
  통과. `build.bat` 이 `setup_pro.iss` AppVersion 을 안 맞춰 프로 설치본이 1.0.38 로 표시되던
  것도 이번에 동기화 블록을 추가해 고쳤다(앱 본체 버전은 version.txt 라 기능 영향 없음).
  ② **target_J(전술창 접기):** 경기 중 하단 전술창이 하단 22% 를 덮어 SKIP 프롬프트 인식을
  막는다(사용자는 마우스로 '−' 를 누른다). 템플릿은 전술 아이콘+'−'(중심=− 버튼), 실전
  1920x1080 프레임에서 추출, 패널 있는 프레임 1.0 / 없는 프레임 ≤0.74 → 임계 0.88. 배경
  PostMessage 클릭이 인매치 HUD 에 먹는지는 **실기 확인 필요**(안 먹으면 패드 대안 검토).
  ⚠️ 런타임 타겟은 `targets.json`(임베드) 이 기준이고 `DEFAULT_TARGET_CONFIGS` 는 폴백이다 —
  target_I 는 DEFAULT 에만 있어 실제론 비활성이었다. 새 타겟은 **둘 다**에 넣는다.
  ③ **홈 화면 OCR:** 경기 결과 패널(0.7초)과 달리 로비 홈 카드는 큰 글자로 오래 떠 있다.
  매치 게이트 실패 프레임에서 3초마다 `HOME_OCR_REGION`(0.13,0.16,0.70,0.47) 을 winocr 로
  읽어 `parse_home_text`: 첫 'OO 감독'=티어(두 번째 '세미프로 2부 감독 승격까지' 무시),
  '랭킹점수' 뒤 숫자만 점수(승격 포인트 '17점' 아님), '순위' 바로 뒤 숫자만 순위('친구
  순위' 탭은 숫자가 없다). 같은 값 2회로 확정해 `_apply_rank_values`(결과 패널과 공유) 로.
  실측: 세미프로 홈 원문 "세미프로3부감독0승/0무/1패9경기남음세미프로2부감독승격까지17점남음".
  챔스 홈은 사용자 캡처(축소본) 기준 합성 문자열로 검증 — **1920 실측 캡처로 재확인 필요.**
  ④ **Codex 2차 의견 + 설치 폴더 실측(컷신 배경 스킵):** `buttonData*.ini` 가 VB_FE_*(메뉴)
  와 VB_AI_*(인게임) 가상 버튼을 분리한다 — '메뉴는 배경에서 되는데 스킵은 안 되는' 이유와
  일치(인게임 계층이 포커스 게이트). customoptions.ini 에 배경 입력 옵션 없음. 안티치트
  (BlackCipher/NGM) 동작 중.
  ⑤ **5각도 리서치 워크플로(웹+코드, 22건→3가설+5개선) 결론:** 유저모드 배경 스킵 가능성
  **5~10%**. 확정 사실: `fczf.exe` 임포트는 DINPUT8+XINPUT9_1_0 뿐(RawInput 없음), 키보드 S 는
  DirectInput 스캔코드(SC_S→VB_AI_A) 인데 DISCL_FOREGROUND 라 배경에서 키보드 장치가
  unacquire 된다 → 's 메시지' 계열(pm_s/char_s/attach_state_s/attach_post_s…)이 전부 sham 과
  같았던 이유가 '메시지 무시'가 아니라 '장치 자체가 죽음'이다. 남은 유일한 미검증 지렛대 =
  **H1 `attach_active_hold_a`**(AttachThreadInput 으로 게임 큐에 붙은 채 SetActiveWindow → 스레드
  로컬 활성 값을 세우고 A 홀드; 게이트가 GetActiveWindow/GetFocus 면 통과, 전역
  GetForegroundWindow 면 불가) — 대조 `attach_hold_a` 와 함께 A 스윕에 넣었다
  (`input_message.run_attached_to_window`, 우리 프로세스가 전면이면 시도 안 함). H2(전 창 최대
  봉투 스푸핑)·H3(`spoof_a_hold` 청정 A/B, sham 2회마다)는 '불가를 통계로 봉인'하는 용도.
  실험 설계 개선 제안(미적용): ① `skip_stats.py`(Wilson/Fisher/SPRT) + 보고 도구 ② 1.5초
  경계 블랙아웃 제거(프레임 링버퍼) ③ sham 블록 무작위화 ④ 층화 대조(0.6/1.2/2.0/3.0)
  ⑤ A 템플릿 2프레임 합의·`dump_window_classes` 실제 호출(현재 호출처 0 = 죽은 코드).
  차선(정책 변경 시): `SKIP_A_FOREGROUND=True` 로 홀드 0.6~0.8초·컷신당 1회 실측해 번쩍임
  최소화, 또는 '안 스킵하는 비용(컷신당 초)'을 로그로 수치화해 데이터로 결정.

- `2026-08-21` **자동 종료 규칙 셋(기본·대량 실점·후반) + 구매자별 설정 — 시계 OCR 도입.**
  사용자 요청: "3점차 이상이면 비율 무시하고 무조건 종료, 70분 이후 1점차로 지고 있으면
  종료, 이 수치를 전부 패널에서 라이센스마다 다르게". 구현:
  ① **규칙은 값이다** — `auto_exit.ExitRules(base_deficit=2, hard_deficit=3, late_minute=70,
  late_deficit=1)`. `classify` 우선순위 hard > base > late. `LossTracker.observe()` 가 종류를
  돌려주고(`feed()` 는 bool 호환 유지), **래치가 둘**이다: 쿼터 규칙(base/late)은 경기당
  한 번(둘 다 같은 '진 판' 카운트 — 따로 걸리면 한 경기가 두 번 세어진다), hard 는 쿼터가
  '방치'로 결정한 **뒤에도** 한 번 더 선다(0:2 방치 판이 0:3 으로 벌어지면 비율 무관하게
  나가라는 뜻). late 비율은 따로 줄 수 있고(별도 ExitQuota), 없으면 base 쿼터를 같이 쓴다.
  ② **경기 시계는 winocr 로 읽는다** — 스코어 숫자와 달리 읽힌다(저장 프레임 8장 전부:
  '84:10'·'62•.04'·'6454'·'90:oo' 같은 형태 → `parse_clock_text` 가 전부 받음). 상대
  닉네임 오른쪽 **흰 가로 박스**를 모양(가로 1.8~5배·밝음·꽉 참)으로 먼저 찾고
  (`find_clock_box`, 위치가 아니라 모양 — 닉네임 길이에 안 흔들림; 숫자 닉네임은 검은
  박스라 안 들어옴) 그 안만 2배·en 으로 OCR. 5초 간격(`AUTO_EXIT_CLOCK_INTERVAL_SECONDS`)
  — SKIP·등수와 같은 워커라 자주 안 돌린다. `ClockTracker` 는 연속 2회 비역행이어야
  확정, 크게 역행하면 확정 해제(새 경기/오독), 60초 부재면 비움. winocr 없으면 late 규칙만
  조용히 꺼진다(minute None → classify 가 late 를 안 낸다).
  ③ **서버는 필드 규격 하나**(`pro_settings.FIELD_SPECS`, 6필드: autoExitRatio·Base/Hard
  Deficit·LateMinute/Deficit·LateRatio) 로 전역(config/pro_settings)·키별(라이센스 문서
  평면 필드)·클라이언트 JSON(`auto_exit_settings` snake_case) 을 전부 만든다.
  `resolveExitSettings` = 키별 → 전역 → 기본값, **필드마다**. 범위는 클라이언트
  `auto_exit.SETTING_BOUNDS` 와 같아야 한다(다르면 서버가 받은 값을 클라이언트가 버려
  "바꿨는데 안 바뀐다"). 서명 밖 운영값이라는 신뢰 모델은 그대로(비율 때와 같은 3중 방어).
  `auto_exit_ratio` 도 같이 보내 구버전 클라이언트 호환. 관리 화면: 전역 패널 6필드
  (빈 정수=미변경, 빈 후반 비율=null 해제), 키별 '종료 설정' 모달(빈 칸=null=전역 따름,
  placeholder 에 전역 실효값), 행에 '맞춤 규칙' 표시. `update({field:null})` 로 해제
  (FieldValue.delete 대신 — 의미 같고 가짜 저장소 테스트 가능).
  ④ 글리프는 0~4(4 는 저장 프레임 4:0·4:1 에서 추출) — 5~9 는 `logs/score_unknown/`
  자동 표본으로 채운다. 3점차는 0:3·1:4 까지 잡히고 0:5 는 아직 SCORE_UNKNOWN(진행 중).
  ⑤ **적대적 리뷰(4관점 탐색·발견마다 반증 2명, 24건 중 상위 5건 검증 → 5건 전부 확정)가
  잡은 것:** (a) 시계 '상향' 단발 오독(1↔7)을 즉시 확정해 **14분 경기에서 후반 규칙이
  서는** 결함 — 연속 확인이 역행 쪽에만 있었다. 상향 점프(>2분)도 역행처럼 다루고 역행
  판정은 pending 이 아니라 **확정값 기준**으로(오독 뒤 정상 복귀 보존). 교훈: '연속 N회'
  는 같은 캐시값을 재사용하는 관측(5초 간격 시계 + 1초 간격 스코어)에 대해 독립 증거가
  아니다. (b) late 규칙이 선행 스코어 방어를 우회 — 정적 (0,1) 픽셀이 선행 증거이자 판정
  근거. 선행 조건을 '그 규칙의 점차보다 **나은** 스코어를 봤나'(`_min_losing_seen`)로
  일반화. (c) late 비율이 따로면(쿼터 둘) 래치도 둘이어야 한다(`shared_quota_latch`).
  (d) 시계 OCR 이 en 언어팩을 전제 → ko 폴백 + 60초 못 읽으면 로그·영역 저장. (e) 그 외
  작은 것들(시작 시 ClockTracker 리셋, settings.ratio 기본값=현재 쿼터 비율, 3자리 시계
  폴백 제거, bool 비율 거부 통일, 서버 전역 읽기 실패 시 키별·기본값 응답, admin 모르는
  action 400, 모달 전역값 0 표시). **미검증 19건은 리뷰 결과 파일에 남겨 뒀다** — 그중
  '필드 간 제약 없음(hard ≤ base 면 비율 무력화)'과 '전역 패널 빈 칸 의미 3가지'는 다음에
  손볼 후보.
  ⑥ **Codex 2차 의견(컷신 배경 스킵):** 유저모드·무깜빡임·무주입으로 엔진의 포커스
  게이트를 넘는 새 가설은 사실상 없다(AttachThreadInput+SetActiveWindow 1% 미만,
  ViGEm 슬롯0 콜드스타트 0.5% 미만). 가능한 길은 전면화뿐이고 최소 전면 시간을 실측
  (16~1200ms × 20회)하라는 제안. 실험 설계 비판: 3초 대조가 초반 입력창에만 반응하는
  후보를 생존자 편향으로 제외, sham 5회 주기는 무작위 20% 배정으로, 입력 함수가 스레드를
  막아 결과창을 소모하는 문제, 독립 종료 신호(HUD 복귀) 필요. 결론은 기존과 같다.

- `2026-08-21` **2점차 자동 종료 비율을 라이센스 키별로 — 전역값 위에 키별 덮어쓰기.**
  구매자 한 명에게만 다른 비율을 주고 싶다(사용자 요청). 라이센스 문서에 `autoExitRatio`
  (0~1, `null`=전역)를 두고 `pro_settings.resolveAutoExitRatio` 가 **키별 → 전역** 순으로
  고른다. verify 는 이미 읽은 라이센스 문서에서 꺼내므로 **추가 Firestore 읽기 0**,
  전역값은 기존 TTL 캐시 그대로. 클라이언트는 손대지 않았다(서명 밖 `auto_exit_ratio`
  필드 하나 그대로 — 3중 방어(서명된 프로 응답에서만·범위 밖 거부·`_is_pro` 재확인)도
  그대로). 관리 화면: 프로 행에 "종료 60% (키별)" / "종료 40% (전역)" 표시 + '종료율'
  버튼(% 입력, 비우면 해제). 서버만 배포하면 되고, **효과는 구매자가 앱을 다시 켜는
  인증 시점**부터다. 해제는 `FieldValue.delete` 대신 `null` 저장 — 의미가 같고(둘 다
  전역), 가짜 저장소로 핸들러를 테스트할 수 있다. `api/admin.js` 도 verify 처럼
  `Module._load` 스텁 테스트(`admin_handler.test.js`)를 신설했다 — 분기를 빠뜨리면
  `disabled` 토글로 떨어지는 조용한 사고를 여기서만 잡는다. 프로 키에만 받는다(일반·
  mPause 문서에 적히면 아무 효과 없이 관리자만 헷갈린다).

- `2026-08-21` **'SKIP 하려면 아무 키나 누르세요' 프롬프트 — OCR 직행 START + ▷ SKIP 수정 실측 재검증.**
  ① **재검증(새 빌드를 게임에 붙이기 전, 저장된 실측으로):** 게이트 빌드(8-14) 로그에서
  target_F/G 감지 **155회 전부 "ESC 보류"**(8-18: 74/74, 8-19: 81/81, 키 입력 0) — 원인
  수치 확정. 7-28(게이트 이전)은 같은 화면에서 `[키] ESC`(=버튼 16=START) 349펄스로
  통과. `dispatch_key_press` 는 이름을 `KEY_TO_GAMEPAD` 로만 매핑하므로 key=start 는
  7-28 과 **바이트 동일한 입력**이다. 같은 커밋(916034f)의 두 번째 게이트
  `_fast_skip_template_visible`(A/S 템플릿 선차단, 매 프레임 1.2초 봉쇄)은 저장된 막힌
  화면 크롭 **804장에서 0장 발동**(아이콘 검증 여유: ▷ SKIP 화면 0.40, '아무 키나' 화면
  0.53 vs 임계 0.65). 남은 것 = 실기 한 판(사용자).
  ② **OCR 직행:** 그 화면을 winocr(ko)이 실제 파이프라인(하단 22%·1280 축소)으로 **한
  프레임 86%** 읽는다(112장). 그런데 읽은 뒤 '일반 스킵 → 비활성 실험'(3초 무입력 대조·
  후보 순환·5번에 1번 대조군=무입력)으로 보내 6초 뒤에야 넘어갔고, 한글을 못 읽는 PC
  에선 침묵했다. 답이 알려진 화면(START)이라 `_anykey_direct_start` 가 오염 판정·3초
  봉쇄보다 **먼저** 가로채 바로 START(2회 연속 확인=0.3초, 0.8초 간격 재누름). 판정은
  `skip_anykey.AnyKeyStartPolicy`(순수 로직). 토큰에 "하려면"·"아무기"를 넣었다 — winocr
  가 '키'를 **'기'로 읽고 Enter 를 놓친 프레임이 실측 전부**였다. 에피소드에 이미 잠긴
  다른 힌트(escape 등)는 존중한다(실험 귀속 불변식 — 기존 테스트가 그걸 고정). 템플릿
  (target_G) 경로는 **그대로** — winocr 없는 구매자 PC 의 유일한 길이라 대체가 아니라
  보강이다. OCR 이 누른 직후 0.5초만 매칭을 멈춰 두 스레드가 같은 버튼을 겹쳐 내는 것만
  막는다. 잔여: 첫 두 읽기가 둘 다 "skip"만 잡히면(≈1~2%) 예전대로 실험/템플릿 경로.
  ⚠️ 실측 해석 함정: '감지 3초 전 크롭' 휴리스틱이 7-28 의 ESC SKIP 크롭을 target_G 화면
  으로, '아무 키나' 크롭을 target_F 화면으로 섞어 붙였다 — 오분류로 보이던 5+3건은 전부
  그 오귀속이었고, 분류기 자체의 교차 오류는 0이었다. 수치를 읽을 땐 OCR 원문을 같이
  본다.

- `2026-08-18` **▷ SKIP 화면 멈춤(구매자 다수 보고) — ESC 보류 게이트 회귀 수정.**
  원인 3중첩: ① target_F/G 의 key 가 "esc"(esc/start 는 같은 버튼 16이라 눌리면 동작)
  ② 8-14 커밋(916034f)의 SKIP 실험용 ESC 보류 게이트가 엄격 모드(기본)에서 esc 이름
  타겟을 **영구 억류** ③ 대신 눌러 줄 OCR 경로는 winocr 없거나 작은 글자를 못 읽는
  구매자 PC 에서 조용히 침묵 → 입력 0. 수정: key 를 진짜 의미인 **"start"** 로 바꿔
  게이트를 설계적으로 우회(+wait 0.5 로 연타 절반화·재시도 여력 유지).
  근거 실측: 7-28 로그에서 target_F 에피소드 30개 중 29개가 0~3초 통과(눌리면 됨),
  target_G 53회 전부 통과 — 게이트 이전엔 잘 되던 회귀임을 통계로 확정.
  **Codex 교차 검증 채택분:** ① target_G 도 같은 게이트에 걸림(함께 수정) ② 초기
  설치본(4a812342)이 targets.json 을 배포했고 느슨한 파일이 내장 설정보다 우선 →
  옛 일반 구매자는 업데이트해도 esc 가 남음 → 양쪽 iss [InstallDelete] 에
  targets.json/target_*.png 제거 추가 ③ wait 1.0 은 재시도를 3회로 줄여 이상치
  복구력 저하 → 0.5. **Codex 지적 중 반증한 것:** "백그라운드 START 불달"(1건
  이상치의 과잉일반화 — 에피소드 통계로 기각). 잔여 관찰 항목: OCR 가능 PC 에서
  OCR 스킵 머신이 _skip_active_until 을 연장하며 실패 후 재시도 없이 선점하는 경로.

- `2026-08-17` **프로 UI: 즉시 종료 버튼 제거 + 골드 팔레트.**
  ① 2점차 자동 종료가 이 기능의 **유일한 트리거**가 됐으므로 수동 버튼을 뺐다
  (사용자 결정). `run_quick_exit` 는 자동 경로가 쓰므로 남기고, `exit_button` 속성은
  '항상 None' 으로 유지한다 — 티어 테스트가 "버튼이 없다"를 이 속성으로 고정하고,
  버튼이 되살아나면 테스트가 막는다.
  ② 프로는 **색으로만** 상위 제품을 구분한다: `PRO_COLORS`(샴페인 골드 5색) 를
  `palette()` 클래스메서드에서 에디션에 따라 덮어쓴다. 구조·레이아웃은 일반과
  동일 — 구조가 갈리면 레이아웃 테스트·유지보수가 두 배가 된다.
  ⚠️ 골드 면 위의 흰 글자는 대비가 무너진다 → `accent_fg` 키를 신설해 강조면 위
  글자색(일반=흰색, 프로=진갈색)을 함께 바꿨다. 하드코딩된 "#FFFFFF" 4곳이 그
  배선이다. 사이드바에 골드 외곽선 PRO 칩, 창 제목은 `edition.display_name()`.
  팔레트는 **인스턴스 속성**으로 고정한다(`self.COLORS = self.palette()`) — 클래스
  속성을 직접 바꾸면 한 프로세스에서 두 에디션을 만드는 테스트가 서로 오염된다.

- `2026-08-17` **종료 마무리 타이밍: 재개 즉시 열고, 그림을 본 뒤 3초 기다렸다 누른다.**
  `EXIT_OPEN_DELAY_SECONDS` 1.0 → **0.0**(되살아나자마자 start), 그리고
  `EXIT_FIRST_PRESS_DELAY_SECONDS = 3.0` 을 새로 세워 `ConfirmSequence` 에 넘긴다.
  ⚠️ **후자는 새 기능이 아니라 포팅 누락 복구다.** mPause 에는 처음부터
  `FOLLOWUP_FIRST_PRESS_DELAY_SECONDS=3.0` 이 있었는데, macro 로 옮길 때
  `first_press_delay_seconds` 인자를 안 넘겨 **기본값 0 으로 동작**하고 있었다 —
  그림이 뜬 순간 바로 눌러서 화면이 자리 잡기 전에 입력이 들어갔다. 인자 하나가
  빠져도 아무 테스트도 안 터졌다는 게 진짜 문제라, `ConfirmSequence` 에 넘어가는
  타이밍 인자 다섯 개를 통째로 고정하는 테스트를 넣었다(`tests/test_exit_feature.py`).
  ⚠️ mPause 는 여전히 열기 지연 1.0 초다 — 같은 게임의 같은 동작이므로, 0 초가
  실전에서 더 낫다고 확인되면 그쪽도 맞춰야 한다(제품이 달라 자동 반영 안 됨).

- `2026-08-17` **자동 종료 조건을 '정확히 0:2' → '2점차 이상 열세'로 일반화.**
  판정이 `상대−나 >= AUTO_EXIT_DEFICIT_GOALS(=2)` 라 0:2 뿐 아니라 0:3, 1:3, 2:4 도
  대상이다. 0:2→0:3 처럼 열세가 깊어져도 연속 판정이 이어지고(같은 경기), 선행
  조건도 '열세 아닌 스코어를 먼저 봤나'로 일반화했다(스코어 단조 증가라 진짜
  열세는 반드시 0점차 구간을 지난다 — 정적 오독 방어 그대로 유지). 2:0 처럼
  **내가** 앞선 2점차는 당연히 제외다.
  ⚠️ **글리프 템플릿이 0·1·2 뿐이라 실전에서 지금 잡히는 열세는 0:2 뿐이다.**
  1:3 같은 스코어는 숫자 3 템플릿이 없어 SCORE_UNKNOWN(진행 중 증거)으로만
  처리된다 — 파일 로그의 '숫자 미상'이 그 신호이며, 해당 화면 스크린샷을 받아
  `score_glyphs.py` 에 3~9 를 추가하면 커버가 즉시 넓어진다. 관리 화면 문구도
  '2점차 자동 종료 비율'로 갱신해 CLI(`npx.cmd vercel --prod --yes`)로 배포했다.

- `2026-08-17` **0:2 자동 종료 비율을 서버 운영 설정으로 — 관리 화면 '프로 운영 설정' 패널.**
  40%가 클라이언트 상수(`AUTO_EXIT_RATIO`)면 조정할 때마다 재빌드가 필요하다 →
  Firestore `config/pro_settings.autoExitRatio` 로 옮기고, 관리 화면에서 %로 저장하면
  **모든 mAuto Pro 가 다음 인증(앱 시작) 때** 받아 간다. 실행 중에는 안 바뀐다 —
  30초 상태 전송은 응답을 안 읽는 기존 설계 그대로다.
  ① **서명에 안 묶는다(의도된 결정).** v2 메시지에 필드를 추가하면 이미 배포된
  클라이언트 전부가 검증 실패한다(메시지 규칙은 양쪽 상수가 정확히 같아야 한다).
  위조로 얻는 것도 '구매자 자신의 종료 비율 조절'뿐이라 신뢰 판정과 무관 — 대신
  3중 방어: **서명으로 프로 티어가 확인된 응답에서만** 읽고(license_client), 0~1 밖
  값은 클램프가 아니라 **거부**(None → 내장 40% 유지, fail-safe)하며, gui 는 `_is_pro`
  를 재확인한다. `sendSignedVerdict` 의 extra 는 핵심 필드보다 **먼저** 펼쳐 서명 대상
  필드를 덮어쓸 수 없다.
  ② **비율 변경은 쿼터 카운트를 보존한다**(`ExitQuota.set_ratio`). 카운트를 지우면
  이미 나간 판이 잊혀 직후 몇 판을 연속으로 나간다(비매너 몰림 — 이 기능의 존재
  이유를 스스로 깬다).
  ③ **verify 마다 Firestore 를 읽지 않는다** — 인스턴스별 60초 TTL 캐시
  (`lib/pro_settings.js`, 할당량 소진 장애 이력). 저장하면 같은 인스턴스는 즉시
  무효화되고 다른 인스턴스는 최대 1분 뒤 따라온다. 읽기 실패는 인증을 막지 않는다.
  ④ 관리 화면 함정: `Number("")===0` 이라 빈 입력을 그냥 넘기면 **'0% 저장'이 조용히
  나간다** → 빈 값·범위 밖은 요청 자체를 안 보낸다(동작 테스트로 고정).
  ⑤ api 핸들러는 firebase-admin 초기화 때문에 테스트 사각지대였다 → `Module._load`
  후킹 스텁으로 `verify_handler.test.js` 신설(배선 누락 = "필드만 조용히 빠짐"을 잡는
  유일한 지점). 배포는 어느 순서든 안전(모르는 필드는 무시되고, 없으면 기본값) —
  단 **효과를 보려면 서버 배포 + 프로 클라이언트 재빌드 둘 다** 필요하다.

- `2026-08-17` **0:2 패배 자동 종료(프로) — 매번이 아니라 40%만 나간다.**
  경기 스코어가 0:2 면 즉시 종료를 자동으로 돌리되, **0:2 판을 따로 세고 그중 40%만**
  나간다(20판이면 8판). 무조건 나가면 비매너 점수가 쌓인다는 사용자 실측이 근거다.
  ① **판정은 전부 순수 로직**(`auto_exit.py`): parse_score / LossTracker / ExitQuota.
  쿼터는 floor(n×0.4) — 난수를 안 쓰는 이유는 표본이 작을 때 종료가 몰리는 걸 막고
  어느 구간을 잘라도 비율을 안 넘기 위해서다. 쿼터는 앱 세션 전체로 이어진다.
  ② **스코어보드 실측(사용자 녹화 1920x1080):** 상단 **왼쪽**, 점수는 콜론 없는
  **박스 두 개**(`팀명 [0][0] 상대명 … [19:05]`). 내 팀=왼쪽. 상대 닉네임이 숫자일 수
  있다(실측 예 "4903") → 파서는 숫자 덩어리가 정확히 2개일 때만 받는다. 시계 방어:
  "0:02"(앞자리 0 두 자리)와 20 초과 값 거부 — 안 막으면 **매 경기 시작 2초에 오탐**.
  ③ **스코어 읽기는 OCR 이 아니라 글리프 템플릿 매칭이다(`score_glyphs.py`).**
  처음엔 winocr 로 갔다가 실전에서 폐기했다 — winocr 는 맥락 없는 숫자 1~2개를
  프레임에 따라 통째로 못 읽는다. 녹화의 '0 0' 은 6배 확대 + 검은 여백 0.6 패딩
  조합으로 겨우 읽었지만, 실전 스크린샷의 '1 2' 는 **완벽하게 조인 크롭에서도**
  모든 조합(확대·이진화·반전·en/ko·pad 스윕)이 빈 문자열이었다. 게임 폰트는
  고정이므로 실물 캡처에서 뽑은 글리프(0·1·2)와의 IoU 비교로 바꿨다 — 결정적이고
  ~1ms 라 OCR 워커 예산도 안 잠식한다. 같은 숫자 IoU ≥0.79, 다른 숫자 ≤0.51(실측),
  임계 0.65. 박스 분리는 연결 요소로 한다(밝기 열 스캔은 팀명 흰 글자가 박스에
  붙으면 오려 낸다 — 실측 결함). 읽기 결과는 3값이다: (a,b) / **SCORE_UNKNOWN**
  (박스는 보이는데 템플릿 없는 숫자 3~9 — '진행 중' 증거로만 쓰고, 없음으로 취급하면
  3:1 화면이 경기 종료로 오인돼 래치가 풀린다) / None(스코어보드 없음).
  검증: 실물 프레임 전부 정답 — 0:0 3장→(0,0), 1:2 실전 스샷→(1,2), 스코어보드
  없는 4장→None (제목표시줄 24/31px 가정 양쪽). 새 숫자가 필요하면 스크린샷을 받아
  `score_glyphs.py` 를 재생성한다(파일 로그 '[자동 종료] 스코어 박스는 보이는데
  숫자 미상' 이 그 신호다).
  ④ **오탐 방어가 겹겹이다:** 3회 연속 확인(다른 스코어가 읽히면 streak 파괴, None 은
  유지 — 리플레이 가림은 정상), 경기당 1회 래치, 60초 연속 부재 시에만 경기 종료로
  간주(짧으면 하프타임을 새 경기로 오인해 이중 카운트). 실행은 수동 버튼과 같은 경로
  (run_quick_exit)라 만료·게이트·busy 검사를 그대로 통과해야 한다.
  ⑤ **적대적 리뷰(3관점·6에이전트)가 확정한 6건을 반영했다:**
  - **'연속 3회'는 픽셀이 변해야 독립 관측이다.** OCR 파이프라인이 결정적이라 정적
    화면 하나의 일관 오독은 3회를 그냥 채운다 → **선행 스코어 규칙** 추가: 같은
    경기에서 0:2 가 **아닌** 스코어(0:0/0:1)가 먼저 읽힌 적이 있어야 확정한다.
    스코어는 단조 증가라 진짜 0:2 는 반드시 0:0 구간을 지난다. 대가: 경기 도중에
    자동화를 켜면 그 경기는 못 센다(보수적 방향). 잔여 위험: 0:0 정독 후 0:1 보드를
    일관 오독하는 특정 PC 조합은 Windows 실측으로 확인해야 한다.
  - **WGC 전용.** region 캡처는 프레임이 임의 사각형이고 정지 화면도 매번 새 seq 를
    받아 두 방어가 동시에 무너진다 → `_auto_exit_allowed` 가 `capture_mode=="wgc"` 요구.
  - **트래커도 세션 수명.** 실행마다 새로 만들면 확정(방치 결정)된 경기 도중
    정지→재시작만으로 같은 경기가 두 번 세어진다 → `resume_observation()` 은 래치를
    유지하고 streak·선행 관측만 비운다.
  - **정지 뒤 잔류 트리거 차단.** winocr 호출(수백 ms) 사이에 정지가 눌리면 확정을
    큐에 넣지 않고, `_run_auto_exit` 는 stop_event·워커 생존을 검사한다(수동 버튼은
    예외 — 명시적 클릭이다).
  - **워커 예산 상한.** pad 사다리를 한 호출에 다 돌면 winocr 3회가 SKIP(0.3초
    데드라인)·등수(0.7초 패널)와 같은 워커를 점유한다 → 호출당 pad 하나 순환 +
    관측 시각을 호출 **후**로 기록.
  - **좀비 OCR 워커 세대 토큰.** join 타임아웃 생존 워커가 재시작 후 부활해 트래커를
    이중 공급하는 것을 세대 번호 불일치로 즉시 종료시킨다.

- `2026-08-17` **일반·프로를 별개 프로그램으로 분리했다(같은 소스, 두 exe).**
  판매·관리를 제품 단위로 하기로 해서, 한 바이너리 + 티어 잠금이 아니라 **설치본을
  둘로** 나눴다. 나누는 지점은 진입점 하나뿐이다:
  `macro_main.py`(=macro) / `macro_pro_main.py`(=macro_pro) 가 `macroapp/edition.py` 에
  제품을 세우고, 나머지 전부가 그 값을 읽는다. 상수를 모듈마다 두면 한 곳만 고치고
  잊는 순간 프로 빌드가 일반 키로 열린다.
  ① **일반 빌드에는 즉시 종료 버튼이 아예 없다**(잠긴 게 아니라 없다).
  ② **서명 후보가 빌드마다 다르다.** 일반: `macro_pro → macro → v1`(프로 키도 받아
  주고, 서버를 되돌려도 안 막힌다). 프로: **`macro_pro` 만** — v1 도 안 받는다.
  받아 주면 서버를 옛 버전으로 되돌리는 것만으로 아무 키나 프로가 열린다.
  ③ ⚠️ **자동 업데이트 기록이 하나뿐이라 그대로 두면 사고가 난다.** `config/version`
  문서 하나를 두 제품이 공유하면, 프로를 릴리스하는 순간 **일반 사용자의 런처가 프로
  설치본을 받아 간다**(오류도 경고도 없이). → `versionDocId()` 로 제품별 문서로 나눴다.
  기존 런처는 product 를 안 보내므로 계속 `config/version` 을 본다 → 영향 0.
  런처·`sign_release.py --product`·관리 화면 드롭다운까지 같은 규칙을 쓴다.
  ④ 진입점 두 개는 시작 실패 진단(무성 크래시 대응)을 통째로 공유한다 —
  `tests/test_no_drift.py` 가 '제품 한 줄 말고는 같아야 한다'를 강제한다.

- `2026-08-17` **프로 티어 도입 — 티어를 서명에 묶고, 종료 기능을 본체로 들여왔다.**
  ① **티어 판정을 새 프로토콜 없이 해결했다.** 서버는 이미 `product` 를 서명 메시지에
  넣고 있었으므로(v2), 프로 키를 `product:"macro_pro"` 문서로 발급하고 **상위 포함 규칙**만
  더했다: `macro` 요청 ← {macro, macro_pro}, `macro_pro` ← {macro_pro} 만, `mpause` ← 자기만.
  클라이언트는 항상 `product="macro"` 로 **한 번만** 묻고, 서명을 `macro_pro → macro → v1`
  순으로 맞춰 본다 — **검증되는 쪽이 곧 티어**다. 티어가 서명에 묶여 있어 응답 조작으로
  올릴 수 없고(테스트로 고정), 요청이 한 번이라 왕복이 늘지도 않는다.
  ⚠️ 처음엔 "프로로 먼저 묻고 거부되면 일반으로 다시"를 생각했는데 **함정이 있었다** —
  제품 불일치 거부도 *서명된* invalid 라, 클라이언트의 "서명된 무효면 저장 키 삭제" 규칙에
  걸려 **일반 사용자의 정품 키가 지워진다.** 그래서 한 번만 묻는 설계로 갔다.
  ② **v1/v2 는 '요청에 product 가 있었는지'로 가른다(문서의 제품이 아니라).** 구버전 mAuto 는
  product 를 안 보내므로 계속 v1 서명을 받는다 — 프로 키를 구버전에 넣어도 일반으로 잘 돈다.
  v2 로 서명할 때만 **문서의 실제 제품**을 넣어 티어를 알린다.
  ③ **클라이언트에 v1 후보를 남겨 뒀다.** 서버를 롤백하면 v1 로 서명해 오는데, 그때 정품
  사용자가 막히면 안 된다(일반 티어로 동작). 되돌림 여지를 코드에 남기는 쪽을 택했다.
  ④ **종료 기능은 본체 원본을 쓴다.** 화면 인식·입력은 `window.InactiveManager` ·
  `matching.find_template_center` · `input_gamepad` · 내장 `target_H` 를 그대로 쓰고,
  사본을 만들지 않았다. 판정 로직만 `exit_core.py` 로 **통째 복사**해서 형제 제품과
  바이트 비교가 가능하게 했다(`tests/test_no_drift.py`). 이미 `ed25519_tiny.py` 가 두 폴더에
  복사돼 있는데 같은지 확인하는 장치가 없었다 — 그것도 같이 묶었다.
  ⑤ **자동화 루프와 배타.** `_exit_gate` 가 서 있는 동안 루프는 매칭·입력을 건너뛴다.
  정지된 화면을 계속 재매칭할 이유가 없고, 되살린 직후 뜨는 확인 버튼을 자동화와 마무리
  단계가 **동시에** 누르면 다음 화면까지 눌러 버린다. 정지가 아니라 '건너뛰기'라 끝나면
  사용자가 시작을 다시 누를 필요가 없다.
  ⑥ **버튼을 시작/정지와 같은 줄에 넣었다.** 줄을 하나 더 쓰면(버튼이든 안내 라벨이든)
  오른쪽 열 세로 예산이 32px 늘어 세션 패널이 눌린다(테스트가 즉시 잡았다). **창을 키워
  해결하면 안 된다** — 1366x768 노트북에서 창이 화면보다 커진다.
  ⚠️ **배포 순서: 서버 먼저 → 그 다음 클라이언트 빌드.** 반대로 하면 프로 키가 거부된다.

- `2026-08-09` **라이센스 응답에 Ed25519 서명 — 난독화가 아니라 이게 크랙을 막는다.** 문제: 서버가 `{"valid": true}`를 **서명 없이** 돌려줘서, `hosts`에 `127.0.0.1 license-server-flame-eta.vercel.app` 한 줄 넣고 `{"valid":true}`만 뱉는 5줄짜리 로컬 서버를 띄우면 **바이너리를 건드리지 않고** 크랙된다. 도메인은 Nuitka로 컴파일해도 평문 문자열로 남으므로 난독화로는 절대 못 막는다. → 서버가 `"license-v1|{verdict}|{hwid}|{nonce}|{exp}"`를 개인키(Vercel env `LICENSE_SIGNING_SEED`)로 서명하고, 클라이언트가 임베드 공개키로 검증한다. **nonce**(매 요청 새로 생성)를 서명에 묶어 재전송을, **hwid**를 묶어 남의 정상 응답 재사용을, **verdict**를 묶어 MITM의 판정 뒤집기를, **exp**를 묶어 만료 연장을 각각 차단. 검증 실패는 전부 `valid=False`로 수렴(fail-closed) — 공개키가 안 박힌 빌드도 거부하므로 **빌드 전 `LICENSE_PUBKEY_HEX` 주입이 필수**다. 메시지 필드는 전부 `[0-9a-z|]`(verdict=고정어, hwid/nonce=hex, exp=정수)라 구분자 주입이 원천 불가. 업데이트 서명 키와 **별개 키**를 쓴다(용도 분리 → 한쪽이 뚫려도 파급 차단). ⚠️ **배포 순서 고정: 서버 재배포 → 그 다음 클라이언트 빌드.** 반대로 하면 정품 사용자가 전부 거부된다(옛 클라이언트는 새 `sig`를 무시하므로 서버 선배포는 안전). 검증: Node `crypto.sign(null,...)`(PKCS8 DER 래핑)과 `ed25519_tiny`가 상호 호환됨을 실측하고, **진짜 서버 코드 → 진짜 클라이언트** 왕복으로 가짜서버·exp변조·재전송 거부까지 확인. 단위 13개 포함 pytest 280개 통과.
  - **적대적 검증(6각도·31에이전트)에서 나온 실제 우회 1건 수정:** `exp`를 시작할 때 한 번만 검사해서, 유효한 단기 키로 1회 시작한 뒤 **랜선 뽑고 재시작만 안 하면** 만료 후에도 무한정 돌았다(`license_info`는 `None`으로만 초기화되고 재대입되지 않았고, 30초 상태 전송은 응답을 읽지도 않아 원격 킬이 불가). 서명 위조는 아니라 무료 크랙은 못 만들지만, **무인 방치가 기본인 채굴 도구**라 체험판·1일권의 서명된 유효기간이 사실상 무력화된다. → `_after_verify`가 서명 검증된 `remaining_seconds`로 `_license_deadline`을 스냅샷하고, 자동화 루프가 매 회 로컬로 강제한다. **네트워크 재검증이 아니라 로컬 검사**인 이유: `exp`가 서명에 묶여 위조 불가하므로 서버를 다시 부를 필요가 없고, 오프라인 정품 사용자를 벌주지 않는다. 스레드 안전: 워커 시작 **전** UI 스레드에서만 쓰고 루프는 읽기만 하므로(happens-before + 단일 참조 대입은 원자적) 락이 필요 없다.

- `2026-08-09` **세션 통계에서 승·무·패와 예상 적립 제거 + 판수를 '패널 소멸 엣지'로 재정의 + 우측 열 세로 예산 정합.** 세 가지가 한 뿌리다. ① **눌림:** 우측 열이 요구하는 693px보다 기본 창이 주는 659px가 **34px 모자라** 세션 패널 아래쪽 카드의 값 라벨이 통째로 `unmapped`됐다(사용자 스크린샷의 빈 `경기 속도`/`예상 적립`). 창을 줄여서가 아니라 **기본 실행에서 100% 재현**된다. 진짜 원인은 `right_column.pack_propagate(False)`(gui.py:841) — 요구 높이가 **1로 보고**돼 Tk도 `minsize`도 부족을 알 수 없었고, 그래서 커밋 `1afe351`이 설정 패널 299px + metrics 117px를 얹었는데도 `minsize(1280,640)`가 방치됐다. ⚠️ **`metrics`에 `expand=True`를 주는 건 no-op**이다(실앱 측정: 전후 카드 높이 `[55,55,21,21]` 동일) — `expand`는 잉여 분배 규칙이라 부족 상황에 개입하지 않는다. 이 오답을 다시 시도하지 말 것. ② **승·무·패를 뺀 이유:** `점수가 올랐으면 승`이라는 추정인데, 그 전제(한 경기당 확정 한 번)가 깨져 있었다. `_commit_rank`이 확정 직후 투표만 비우고 패널은 그대로라 **0.3초마다 재확정**되고, 유일한 방어인 45초 dedup 때문에 **결과 화면에 머물면 45초마다 유령 '무'가 +1**, 반대로 45초 안에 온 진짜 다음 경기는 `return None`이 `_previous_score` 갱신 전에 빠져나가 **통째로 유실**됐다(부호가 반대라 총합이 그럴듯해 서로를 가림). 예상 적립도 같은 승패 추정에 얹혀 있고 티어 OCR 실패 시 승리도 0 FC로 계산돼 체계적으로 낮았다. 정확한 값은 'FC 채굴 현황'이 넥슨 공식 기록으로 이미 보여준다. ③ **판수 재정의:** `MatchCounter`가 등수 패널의 **소멸 전이 1회 = 1판**으로 센다(확정이 한 번도 없던 화면은 세지 않아 패널 오검출은 0판이지 유령 1판이 아니다). 같은 패널을 몇 번 재확정하든 1판이고 다음 경기는 패널이 새로 떠야 하므로 시간 간격과 무관하다 → 위 두 버그가 동시에 사라진다. ④ **오늘 판수:** `macroapp/daily_stats.py`(순수 로직, Mac 검증)가 KST 하루를 반열림 구간으로 계산하고, 원장은 `mining_stats.db`의 `macro_matches`에 **UTC로만** 적는다 — 기록 시점 날짜를 컬럼으로 굳히면 시계 보정 후 영구히 틀어져 재집계가 불가능하다(`mining.py`가 넥슨 기록에 쓰는 방식과 동일). 쓰기는 전용 라이터 스레드 뒤에 두어 OCR 워커가 동기화 워커의 `executemany`에 막히지 않게 했고, 저장소를 못 열면 통계만 끄고 본체는 돈다(오늘 값은 `0`이 아니라 `—`). 검증: 신설 `tests/test_ui_layout.py`가 두 페이지 × 세 크기에서 **서브트리 재귀**로 `ismapped and h>=reqh and w>=reqw`를 검사한다(4개 라벨만 보면 새 위젯이 잘려도 통과해버린다 — pack은 마지막 슬레이브부터 자르므로). pytest 98개 통과.

- `2026-08-09` **SKIP 실험에 가짜 입력 대조군(sham) 추가 — 성공률이 상대의 스킵과 구분되지 않던 문제.** 컷신은 **상대가 눌러도 양쪽 다 끝난다.** 그래서 "입력 후 1.5초 안에 프롬프트가 사라짐 = 성공"은 우리 입력의 효과와 상대/자연 종료를 섞어 센다. 2026-07-28 실측(1239건)을 다시 분석하니 이게 실제 문제였다: 대조 구간 중 종료(`opponent_or_natural_exit_during_control`)가 127건이고, 이걸 지수분포로 외삽한 기준선이 **약 10%**인데 `attach_state_s_hold`(3/28=10.7%)·`char_s`(11.1%)·`pm_s_hold`(11.1%) 등 다수 후보가 **기준선과 통계적으로 구분 불가**(p=0.56)였다. 반면 `char_s_hold`은 35/61=57.4%(16시 구간만 보면 31/32=97%, p=5e-30)로 기준선을 크게 넘었고 지연 중앙값도 0.08초(다른 후보 0.7~1.2초)로 달랐다 — 그러나 **이 10%는 측정값이 아니라 추정값**이라 확정 불가. → 후보 목록에 `control_noop`을 넣어 `SKIP_SHAM_EVERY`(기본 5) 에피소드마다 **대조 관찰·가드·판정 창을 똑같이 거치면서 입력만 보내지 않는** 대조군을 돌린다. 그 성공률이 같은 조건·같은 상대 풀에서 실측한 기준선이며, 후보와의 차이가 순수 효과다. 부수 효과로 **AI전이 깨끗한 실험장인지도 이 값이 답한다**(0%면 아무도 대신 안 누름 = 이후 성공은 전부 우리 것). 구현 주의: ① 대조군은 절대 학습·선호 후보가 되면 안 됨(매크로가 입력을 멈춤) ② 일반 스윕 순환에서 제외해야 함(안 빼면 스윕이 대조군을 한 자리로 세어 기준선 표본만 과다) ③ `_advance_past`도 건너뛰어야 진짜 후보 차례를 안 잡아먹음 — ②는 테스트가 잡아낸 실제 버그. 검증: 순수 로직이라 Windows 없이 단위 검증(대조군 주기·학습 금지·스윕 순서 보존·비활성화), pytest 83개 통과.

- `2026-08-02` **UI 모던 리디자인 + '저부하 엔진'·'빠른 실행' 패널 제거.** 팔레트를 네이비/인디고 → **뉴트럴 다크(#0A0D14) + 단일 강조색(#635BFF)**으로 교체(채도를 낮춰 장시간 사용에 편하고 정보 위계가 또렷해짐). tkinter 기본 위젯이 옛 윈도우 느낌이라 **`tk.Scale`→캔버스 슬라이더**(둥근 트랙+손잡이), **`tk.Radiobutton`→세그먼트 토글**로 교체(`variable.trace_add`로 `on_capture_mode_changed` 같은 코드 변경도 강조가 따라옴). 패널 제목은 좌측 정렬 + 강조 바로 통일(`mining_ui`도 동일 `_card_title`). **레이아웃:** 두 패널을 없애 오른쪽 열에 큰 여백이 생겨서 **기본 설정을 오른쪽으로 이동**(왼쪽=타겟 표 전체 높이, 오른쪽=세션·설정·시작/정지). 타겟 행은 `fill=BOTH+expand`로 남는 높이를 나눠 가져 표처럼 보이게 함. 상태 문구는 의미별 색(정상/경고/오류/대기)으로 사이드바 점과 함께 칠함. **작은 창 대응(실측):** 오른쪽 열이 고정폭이라 `minsize`를 1280x640으로 올렸고, 시계는 `place`(자연 크기 0 → 잘림) 대신 `pack(expand)` + `<Configure>` 폰트 자동 축소, 상태 줄은 `side=BOTTOM`으로 먼저 고정해 눌려도 사라지지 않게 함. 라이센스 다이얼로그도 같은 팔레트로 교체(첫인상 통일). 검증: compileall·pyflakes·pytest 55개 통과 + PrintWindow 스냅샷(기본/최소 크기, 두 페이지)과 위젯 상호작용 스모크(슬라이더 클램프·세그먼트 연동·버튼 활성색·상태색·내비 강조) 전부 통과.

- `2026-07-12` **(A) SKIP 스윕에 'hold-to-skip' 축 추가 + 잘못된 '다른 동작' 후보 폐기.** 배경 컷신 스킵을 웹 리서치+적대적 검증(워크플로 5각도, 14후보 전멸)한 결과 재확인: 전면화0+주입0 유저모드로 스킵 입력을 게임플레이 계층에 넣는 건 원리적 불가(게임이 매 프레임 GetForegroundWindow 재검증, 전역값이라 외부 스푸핑 불가; 주입만 가능하나 안티치트·스코프 밖). **처음엔 '개발자가 안 막은 다른 동작'(start/back로 pause 메뉴, esc/enter/space로 CEF)을 사냥하려 했으나 → 사용자 정정: 컷신 스킵을 넘기는 입력은 오직 '키보드 s' 또는 '게임패드 A' 뿐(다른 버튼·키·pause 무효).** 그래서 그 후보들 전부 폐기. **살아남은 진짜 인사이트 = hold-to-skip**: 사용자 실측 "전면에서 'A 홀드'로 넘어감" → 원래 스윕이 0.15초 '탭'만 해서 홀드 축이 빠져 있었음(탭은 원리상 못 넘김). 스윕을 [스킵 입력=s/A] × [전달 경로] × [탭/홀드]로 재구성: `char_s`/`attach_state_s`/`attach_post_s`/`pm_s`(s 딥 메시지 — 게임이 CEF 메시지 계층으로 s를 읽으면 배경 통과 가능)·`focus_child_s`(자식 SetFocus)·`a`(vgamepad A=게임 A 확정), 각각 `*_hold`(1초, `SKIP_A_SWEEP_HOLD_SECONDS`) 변형 포함 = 12후보. si_s/focus_s는 유출/깜빡임이라 스윕 제외. 물리 키보드는 배경에 더 나쁨(키보드=포그라운드 전용 라우팅, XInput=세션 전역). `_press_skip_candidate`는 `<방법>_<키>` 파싱으로 일반화(홀드/딜리버리 분기 정리용, 현재 key='s'만). 변경: `config.py`(스윕/`SKIP_A_SWEEP_HOLD_SECONDS`), `gui.py`(`_hold`+키보드 일반화). compileall/pyflakes 통과(신규 경고 0), 라우팅 시뮬 검증. **Windows 실측 필요** — 통하는 게 있으면 배경 스킵 확정, 없으면 게임플레이 계층이 순수 RawInput 전면 전용(배경 불가 확정).

- `2026-07-07` **(A) SKIP GitHub 리서치 결론.** 권위 출처(MS DirectXTK Wiki, WGI 문서, reWASD·GlosSI)로 확정: **XInput 값은 배경에도 들어감**(메뉴 A가 배경에서 되는 이유), 게이팅은 **게임/런타임이 포커스로 스스로** 함(DirectXTK: "applications should ignore input when in background"). 외부 유저모드 도구(vgamepad/GlosSI/XOutput/reWASD)는 값만 넣지 게이팅을 못 지움 → **진짜 배경 우회 불가**(게임 exe 패치/DLL 후킹만 되나 안티치트·스코프 밖, 절대 금지). **단 유일한 미검증 배경 후보 = `WM_ACTIVATEAPP`+`WM_ACTIVATE` 활성 플래그 스푸핑**(SDL #4450: 앱이 메시지만으로 '포커스 있다' 오인). 엔진이 스레드-로컬 플래그로 게이팅하면 배경 스킵 가능, GetForegroundWindow 직접 확인이면 불가 — 실측만이 판별. → `SKIP_A_ACTIVATE_SPOOF`(기본 켬, 깜빡임0) 먼저, 안 되면 전면화(ALT트릭+SetForegroundWindow, 리서치가 보증한 유일 유저모드 방법) 폴백. RDP 별도세션은 안티치트 차단 우려로 제외.
- `2026-07-07` **(A) SKIP 최종 결론.** 실측으로 두 입력 계층 확정: 메뉴(패스/확인=VB_AI_A)는 **게임패드로 배경에서 됨**(DirectInput/buttonData), 그러나 **컷신 스킵은 매치 엔진이 '전면(포커스)'일 때만** 읽음. 사용자 직접 확인: "A 홀드 + 알트탭으로 FIFA 전면화해야 스킵됨". 결론 = **컷신 스킵은 배경으로 원리적 불가**(키보드·게임패드·커널·하드웨어 전부, OS가 게임플레이 입력을 전면 창으로만 라우팅). 유일 동작 = **잠깐 전면화+A 홀드+원래 창 복원**(사용자 알트탭 자동화, `SKIP_A_FOREGROUND`, 기본 꺼짐 — 화면 변화 0 유지, 켜면 컷신마다 ~1초 FIFA 번쩍). buttonData: ViGEm 패드는 Controller_011(XUSB) 매핑, A=Button01→VB_AI_A. XUSB↔DInput 번호 어긋남(Button08→RT, Button10→START)이라 vgamepad START는 게임에서 RT로 받힘. 배경 키보드/스윕 코드는 불가 증명돼 (A)브랜치에서 제거.

- `2026-07-03` **깜빡임 0 요구** 반영(자동화라 화면 변화 절대 불가). 적대적 리뷰로 확인: `focus_s`(SetFocus)는 백그라운드 top-level을 activate시켜 **반드시 깜빡임** → 기본 스윕에서 제외(수동 opt-in만). `si_s`(SendInput 전역)는 깜빡임 0이나 **활성 앱에 s 유출** → 제외. 기본 스윕 = 깜빡임0·유출0 4종: `attach_state_s`(AttachThreadInput+SetKeyboardState, GetKeyState 폴링 속이기), `char_s`(WM_KEYDOWN+WM_CHAR+WM_KEYUP 딥, CEF용), `attach_post_s`(큐 공유 후 SetFocus 없이 포커스창에 Post), `pm_s`(WM_KEYDOWN 딥). **경계:** 게임이 RawInput/GetAsyncKeyState 포그라운드 전용이면 유저모드론 원리적 불가(커널 드라이버만) → 첫 (A) SKIP에 창 클래스 덤프 로그(`[SKIP 진단]`)로 CEF(Chrome_RenderWidgetHostHWND) 여부 판별. ⚠️ `send_key_focus_attach` docstring이 '화면 변화 0'이라 거짓 표기했던 것 정정.
- `2026-07-02` (A) SKIP = **자동 버튼 탐색(스윕)+학습**으로 전환, 전면전환 최후수단은 **기본 꺼짐(=0)**. **이유:** 실측 — 가상 A는 탭·1s 홀드 모두 무시, 클릭 무효, 전면전환+키보드 s '탭'은 통함 → 프롬프트는 탭 기반이고 게임의 'A 동작'이 XInput A와 어긋난 것(자체 매핑/DirectInput 인덱스 추정). 후보(a,b,x,y,rb,lb,rt,lt,down,pm_s)를 사이클당 1개 탭 → 스킵이 사라지면 그 입력을 학습(로그 "[SKIP] 학습 완료"). 사용자가 비활성 100%를 요구 → sendinput은 옵트인(`SKIP_A_SENDINPUT_AFTER_SECONDS>0`)일 때만, 스윕 소진+시간 경과 후. 답을 알면 `SKIP_A_BUTTON="b"`처럼 고정. 스킵 실패해도 컷신은 자연 종료라 안전.
- `2026-06-29` 등수/티어/점수를 **각각 독립 "마지막 값 유지"**. **이유:** 로비(등수 없음) 읽기가 매칭 때 잡은 등수를 덮어쓰던 버그. 챔/슈챔이면 등수가 무조건 있음.
- `2026-06-29` 목표 도달 자동 정지(등수 이내 / 점수 이상 / 안 함). 컨센서스로 확정된 값에서만 판정.
- `2026-06-29` 등수 OCR을 **프레임 신선도 게이팅 + 컨센서스 투표 + 전처리(업스케일·대비, 이진화 금지)**로 개선. **이유:** 패널이 ~0.7초만 떠서 시간 간격 폴링은 통째로 놓침. 확정은 벽시계로 폴링(WGC 정적화면 프레임 동결 대비).
- `2026-06-29` vgamepad 입력 시퀀스를 **RLock로 직렬화**. **이유:** 매칭 루프와 SKIP 워커가 공유 가상패드 리포트를 동시에 만져 입력 병합/누락(경합 ~9%).
- `2026-06` OCR을 **별도 스레드**로 분리. **이유:** OCR이 이미지 매칭 루프를 막아 인식 속도 저하.
- (초기) **Nuitka** 채택(+PyInstaller 폴백). **이유:** 디컴파일 방어(판매 보호). `sys.frozen` 대신 `__compiled__` 설정됨.
- (초기) 타깃 이미지·`targets.json`을 **바이너리에 임베드**(`gen_assets.py`→`_assets.py`). **이유:** 설치 폴더에 자산 노출 안 함.
- (초기) HWID = **SMBIOS UUID → MachineGuid(레지스트리) → MAC** 순 폴백, sha256[:32]. 라이센스 = Vercel + Firestore, `BOT_API_KEY` 인증.
- (초기) 배경 입력 = **WGC 클라이언트 영역 캡처 + WM_ACTIVATE 가짜 포커스 + vgamepad(XInput 전역)**.
- (초기) 매칭 = **2단계(다운스케일 사전필터 → ROI 정밀)**, 화면·템플릿 **둘 다 INTER_AREA**로 축소.

---

## 🧠 교훈 & 실수 노트 (에이전트가 갱신)

- **git 루트는 상위 폴더다.** `git show HEAD:macroapp/x.py`는 빈 결과(에러) → `macro/macroapp/x.py`로 써야 함. `git diff --stat`으로 접두사 확인. 한때 "커밋 누락?"으로 오인했음.
- **winocr/vgamepad/WGC는 Mac에서 못 돌린다.** → ocr.py처럼 **순수 로직(정규식·투표·전처리)을 winocr 호출과 분리**해 Mac에서 단위 검증. 인식 정확도/타이밍 실측은 항상 사용자가 Windows에서.
- **다운스케일은 화면·템플릿 둘 다 같은 보간(INTER_AREA)**이어야 함. 한쪽만 스트라이드 슬라이싱하면 얇은 템플릿이 1차에서 누락(8개 중 6개 실패) 버그.
- **cmd는 글롭을 안 펼침** → `py_compile macroapp\*.py` 실패("문법검사 실패"). `compileall`(디렉터리 처리) 사용.
- **`taskkill` 은 즉시 반환하지만 파일 핸들 해제는 늦다.** 바로 이어진 `rmdir /s /q` 가
  "Access is denied" 로 **일부만** 지우고, 남은 껍데기 폴더가 `ren` 을 조용히 실패시킨다
  (`ren` 은 대상이 있으면 에러 없이 아무 일도 안 한다). 그러면 새 빌드는
  `*_main.dist` 에 남고 잔해 폴더의 **옛 exe** 가 기존 가드(`if exist ...\x.exe`)를
  통과해 버린다 → 2026-08-17 실측: 프로 설치본이 ISCC 에서 죽었는데 종료코드를 안 봐서
  "빌드 완료" 로 끝났다. **교훈 세 줄:** ① 삭제 후엔 exe 가 아니라 **폴더 자체**가
  사라졌는지 본다 ② `ren` 뒤에 원본 폴더가 남았는지 확인한다 ③ 외부 도구(ISCC 등)는
  호출마다 `if %errorlevel% neq 0` 로 받는다. 성공 메시지는 **모든** 산출물이 있을 때만.
- **빌드 산출물의 '신선도'는 타임스탬프로 판단하면 안 된다.** Nuitka standalone 은
  의존성(Tcl/Tk·numpy·cv2)을 원본 mtime 그대로 복사해서 1500여 개가 '옛 파일'로 보인다.
  대신 **새 상수/심볼이 exe 바이트에 있는지**로 확인한다
  (예: `grep -c AUTO_EXIT_DEFICIT_GOALS dist\macro_pro_app\macro_pro.exe`, 옛 상수는 0 이어야 함).
- **vgamepad는 `import`만 해도 ViGEm 없으면 크래시** → 빌드 점검은 `import` 말고 `importlib.util.find_spec`.
- **GIL이 보호하는 것에 헛수정 금지:** 단일 참조/튜플 대입은 원자적. 동시성 리뷰의 오탐 다수가 여기. 진짜 경합은 "공유 가변 객체를 두 스레드가 동시 수정"하는 경우(예: 가상패드 리포트, 컨센서스 객체 공유).
- **'비활성 키보드 입력'은 원리적으로 불가.** RawInput 게임은 포커스 창에만 키가 가서 PostMessage 키(WM_KEYDOWN)를 무시한다 — 그래서 배경 입력의 근거가 전역 XInput(vgamepad)이다. "가상 버튼이 안 먹는다"는 증상은 (1) hold-to-skip인데 탭만 함(롱홀드부터 의심) (2) 게임이 그 버튼만 무시 순으로 가르고, 최후수단은 전면 순간전환+SendInput 스캔코드(sizeof(INPUT)=40, x64).
- 좌표·화면 레이아웃은 **사용자 스크린샷으로 확정**한 뒤 값을 박는다(추측 빌드 금지).
